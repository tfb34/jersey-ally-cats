---
interface ContainerProps {
  class?: string;
}

const { class: extraClasses } = Astro.props;
---

<div
  class:list={[extraClasses, "w-full max-w-7xl px-4 sm:px-6 lg:px-8 mx-auto"]}
>
  <slot />
</div>
